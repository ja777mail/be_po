name: Run Newman Collection

on:
  workflow_dispatch:

jobs:
  run-collection:
    runs-on: ubuntu-latest
    steps:
      # 1️⃣ Скачиваем репозиторий
      - name: Checkout repository
        uses: actions/checkout@v3

      # 2️⃣ Настраиваем Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20

      # 3️⃣ Устанавливаем Newman и HTML репортер
      - name: Install Newman and HTML Reporter
        run: |
          echo "📦 Installing Newman and HTML reporter..."
          npm install -g newman@5 newman-reporter-html@5

      # 4️⃣ Создаем папку для отчета
      - name: Prepare report folder
        run: |
          echo "📂 Creating report folder..."
          mkdir -p newman-report

      # 5️⃣ Запускаем коллекцию Newman с CLI и HTML отчетом
      - name: Run Newman Collection
        run: |
          echo "🏃 Running Newman collection..."
          newman run TestAPICollection.json \
            --reporters cli,html \
            --reporter-html-export newman-report/report.html

      # 6️⃣ Проверяем, что отчет действительно создан
      - name: Verify report
        run: |
          echo "📋 Checking report files..."
          ls -la newman-report

      # 7️⃣ Загружаем HTML-отчет как артефакт
      - name: Upload Newman HTML Report
        uses: actions/upload-artifact@v3
        with:
          name: newman-html-report
          path: newman-report/report.html
