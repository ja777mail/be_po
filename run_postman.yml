name: Run Newman Collection

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  run-collection:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Клонируем репозиторий
      - name: Checkout repository
        uses: actions/checkout@v3

      # 2️⃣ Настраиваем Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20

      # 3️⃣ Устанавливаем Newman
      - name: Install Newman
        run: |
          set -x
          echo "📦 Installing Newman globally..."
          npm install -g newman
          npm list -g --depth=0

      # 4️⃣ Запуск Newman с подробным логом и HTML-отчётом
      - name: Run Newman Collection
        run: |
          set -x
          echo "📂 Creating report directory..."
          mkdir -p newman-report
          echo "🏃 Running Newman collection..."
          newman run TestAPICollection.json \
            --reporters cli,html \
            --reporter-html-export newman-report/report.html \
            --verbose
          echo "📋 Listing report files:"
          ls -la newman-report

      # 5️⃣ Загружаем HTML-отчёт как артефакт
      - name: Upload Newman HTML Report
        uses: actions/upload-artifact@v3
        with:
          name: newman-html-report
          path: newman-report/report.html
