name: Newman API Tests

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  run-collection:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Получаем код репозитория
      - name: Checkout repository
        uses: actions/checkout@v3

      # 2️⃣ Устанавливаем Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20

      # 3️⃣ Устанавливаем Newman глобально и проверяем версию
      - name: Install Newman globally
        run: |
          echo "🚀 Installing Newman..."
          npm install -g newman
          echo "📌 Newman version:"
          newman -v

      # 4️⃣ Запуск коллекции с CLI + HTML репортом
      #    и пошаговым выводом каждого запроса/ответа
      - name: Run Newman Collection with HTML report and detailed logs
        run: |
          echo "📂 Creating report directory..."
          mkdir -p newman-report

          echo "🏃 Running Newman collection with detailed logs..."
          newman run TestAPICollection.json \
            --reporters cli,html \
            --reporter-cli-no-summary false \
            --reporter-html-export newman-report/report.html \
            --verbose \
            --bail \
            --color on

          echo "📋 Listing report files:"
          ls -la newman-report

      # 5️⃣ Загружаем HTML репорт как артефакт workflow
      - name: Upload Newman HTML report
        uses: actions/upload-artifact@v3
        with:
          name: newman-html-report
          path: newman-report/report.html
