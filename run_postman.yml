name: Run Newman Collection

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  run-collection:
    runs-on: ubuntu-24.04

    steps:
      # 1️⃣ Клонируем репозиторий
      - name: Checkout repository
        uses: actions/checkout@v3

      # 2️⃣ Устанавливаем Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20

      # 3️⃣ Устанавливаем Newman глобально
      - name: Install Newman
        run: |
          echo "📦 Installing Newman globally..."
          npm install -g newman

      # 4️⃣ Запускаем коллекцию с логами, HTML-отчётом и пошаговым выводом
      - name: Run Newman with detailed logs
        run: |
          set -x  # 🔹 Показывать каждую команду shell

          echo "📂 Creating report directory..."
          mkdir -p newman-report

          echo "🏃 Running Newman collection..."
          newman run TestAPICollection.json \
            --reporters cli,html \
            --reporter-html-export newman-report/report.html \
            --verbose

          echo "📋 Listing report files:"
          ls -la newman-report

      # 5️⃣ Загружаем HTML-отчёт как артефакт
      - name: Upload Newman HTML report
        uses: actions/upload-artifact@v3
        with:
          name: newman-html-report
          path: newman-report/report.html
